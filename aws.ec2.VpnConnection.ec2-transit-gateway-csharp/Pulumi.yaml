name: ${PROJECT}
runtime: dotnet
description: ${DESCRIPTION}
template:
  description: EC2 Transit Gateway
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
