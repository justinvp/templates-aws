name: ${PROJECT}
runtime: python
description: ${DESCRIPTION}
template:
  description: Aurora MySQL 1.x (MySQL 5.6)
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
