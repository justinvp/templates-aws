name: ${PROJECT}
runtime: python
description: ${DESCRIPTION}
template:
  description: Public Subdomain Zone
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
