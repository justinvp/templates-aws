name: ${PROJECT}
runtime: go
description: ${DESCRIPTION}
template:
  description: Lookup by DHCP Options ID
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
