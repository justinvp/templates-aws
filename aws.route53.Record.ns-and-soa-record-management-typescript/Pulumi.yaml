name: ${PROJECT}
runtime: nodejs
description: ${DESCRIPTION}
template:
  description: NS and SOA Record Management
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
