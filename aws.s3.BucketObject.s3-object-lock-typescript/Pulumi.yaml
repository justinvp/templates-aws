name: ${PROJECT}
runtime: nodejs
description: ${DESCRIPTION}
template:
  description: S3 Object Lock
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
