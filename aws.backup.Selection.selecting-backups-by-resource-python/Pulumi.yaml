name: ${PROJECT}
runtime: python
description: ${DESCRIPTION}
template:
  description: Selecting Backups By Resource
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
