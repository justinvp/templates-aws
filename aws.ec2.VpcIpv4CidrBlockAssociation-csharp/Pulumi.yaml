name: ${PROJECT}
runtime: dotnet
description: ${DESCRIPTION}
template:
  description: An AWS VpcIpv4CidrBlockAssociation C# Pulumi program
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
