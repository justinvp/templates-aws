name: ${PROJECT}
runtime: go
description: ${DESCRIPTION}
template:
  description: Conditional Trigger with Crawler Action
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
