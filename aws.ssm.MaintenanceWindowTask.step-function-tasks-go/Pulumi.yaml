name: ${PROJECT}
runtime: go
description: ${DESCRIPTION}
template:
  description: Step Function Tasks
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
