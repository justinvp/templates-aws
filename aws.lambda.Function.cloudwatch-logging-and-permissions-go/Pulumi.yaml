name: ${PROJECT}
runtime: go
description: ${DESCRIPTION}
template:
  description: CloudWatch Logging and Permissions
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
