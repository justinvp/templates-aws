name: ${PROJECT}
runtime: nodejs
description: ${DESCRIPTION}
template:
  description: Search By Tags (EC2-Classic or VPC)
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
