name: ${PROJECT}
runtime: nodejs
description: ${DESCRIPTION}
template:
  description: Add inventory configuration with S3 bucket object prefix
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
