name: ${PROJECT}
runtime: dotnet
description: ${DESCRIPTION}
template:
  description: Example IAM Role for EKS Fargate Profile
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
