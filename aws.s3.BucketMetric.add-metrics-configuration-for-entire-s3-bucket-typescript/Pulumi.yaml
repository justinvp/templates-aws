name: ${PROJECT}
runtime: nodejs
description: ${DESCRIPTION}
template:
  description: Add metrics configuration for entire S3 bucket
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
