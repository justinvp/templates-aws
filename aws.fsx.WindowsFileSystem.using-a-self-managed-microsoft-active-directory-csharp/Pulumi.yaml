name: ${PROJECT}
runtime: dotnet
description: ${DESCRIPTION}
template:
  description: Using a Self-Managed Microsoft Active Directory
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
