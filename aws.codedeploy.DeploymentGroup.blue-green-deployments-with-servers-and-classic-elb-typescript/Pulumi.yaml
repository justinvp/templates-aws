name: ${PROJECT}
runtime: nodejs
description: ${DESCRIPTION}
template:
  description: Blue Green Deployments with Servers and Classic ELB
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
