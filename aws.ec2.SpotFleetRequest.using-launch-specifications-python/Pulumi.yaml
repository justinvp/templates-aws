name: ${PROJECT}
runtime: python
description: ${DESCRIPTION}
template:
  description: Using launch specifications
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
