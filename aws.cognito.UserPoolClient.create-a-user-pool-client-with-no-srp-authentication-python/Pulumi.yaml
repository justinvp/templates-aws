name: ${PROJECT}
runtime: python
description: ${DESCRIPTION}
template:
  description: Create a user pool client with no SRP authentication
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
