name: ${PROJECT}
runtime: python
description: ${DESCRIPTION}
template:
  description: Enable Logging
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
