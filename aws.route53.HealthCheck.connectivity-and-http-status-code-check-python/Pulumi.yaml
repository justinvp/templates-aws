name: ${PROJECT}
runtime: python
description: ${DESCRIPTION}
template:
  description: Connectivity and HTTP Status Code Check
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
