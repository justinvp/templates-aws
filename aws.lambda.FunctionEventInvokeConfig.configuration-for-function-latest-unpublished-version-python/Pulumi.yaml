name: ${PROJECT}
runtime: python
description: ${DESCRIPTION}
template:
  description: Configuration for Function Latest Unpublished Version
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
