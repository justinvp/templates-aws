name: ${PROJECT}
runtime: nodejs
description: ${DESCRIPTION}
template:
  description: Instance Target Group
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
