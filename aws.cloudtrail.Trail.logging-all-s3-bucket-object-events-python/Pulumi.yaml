name: ${PROJECT}
runtime: python
description: ${DESCRIPTION}
template:
  description: Logging All S3 Bucket Object Events
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
