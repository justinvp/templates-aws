name: ${PROJECT}
runtime: python
description: ${DESCRIPTION}
template:
  description: Logging All Lambda Function Invocations
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
