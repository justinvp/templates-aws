name: ${PROJECT}
runtime: go
description: ${DESCRIPTION}
template:
  description: Connectivity and String Matching Check
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
