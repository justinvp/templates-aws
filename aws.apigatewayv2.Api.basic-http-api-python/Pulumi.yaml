name: ${PROJECT}
runtime: python
description: ${DESCRIPTION}
template:
  description: Basic HTTP API
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
