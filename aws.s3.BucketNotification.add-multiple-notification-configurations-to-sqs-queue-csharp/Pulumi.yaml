name: ${PROJECT}
runtime: dotnet
description: ${DESCRIPTION}
template:
  description: Add multiple notification configurations to SQS Queue
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
