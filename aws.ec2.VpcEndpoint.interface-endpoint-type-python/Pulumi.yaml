name: ${PROJECT}
runtime: python
description: ${DESCRIPTION}
template:
  description: Interface Endpoint Type
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
