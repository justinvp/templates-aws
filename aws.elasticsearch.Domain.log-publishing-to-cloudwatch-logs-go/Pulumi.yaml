name: ${PROJECT}
runtime: go
description: ${DESCRIPTION}
template:
  description: Log Publishing to CloudWatch Logs
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
