name: ${PROJECT}
runtime: python
description: ${DESCRIPTION}
template:
  description: Aurora Read Replica Autoscaling
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
