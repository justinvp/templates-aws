name: ${PROJECT}
runtime: dotnet
description: ${DESCRIPTION}
template:
  description: Add inventory configuration
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
