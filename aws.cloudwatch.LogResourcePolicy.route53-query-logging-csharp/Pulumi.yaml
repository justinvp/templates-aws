name: ${PROJECT}
runtime: dotnet
description: ${DESCRIPTION}
template:
  description: Route53 Query Logging
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
