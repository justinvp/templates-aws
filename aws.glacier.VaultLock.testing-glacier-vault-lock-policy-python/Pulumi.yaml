name: ${PROJECT}
runtime: python
description: ${DESCRIPTION}
template:
  description: Testing Glacier Vault Lock Policy
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
