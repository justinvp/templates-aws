name: ${PROJECT}
runtime: dotnet
description: ${DESCRIPTION}
template:
  description: Create Cached iSCSI Volume From Snapshot
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
