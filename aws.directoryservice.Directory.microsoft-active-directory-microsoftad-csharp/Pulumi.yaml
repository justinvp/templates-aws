name: ${PROJECT}
runtime: dotnet
description: ${DESCRIPTION}
template:
  description: Microsoft Active Directory (MicrosoftAD)
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
