name: ${PROJECT}
runtime: dotnet
description: ${DESCRIPTION}
template:
  description: Permanently Applying Glacier Vault Lock Policy
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
