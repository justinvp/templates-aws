name: ${PROJECT}
runtime: dotnet
description: ${DESCRIPTION}
template:
  description: Server Side Encryption with S3 Default Master Key
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
