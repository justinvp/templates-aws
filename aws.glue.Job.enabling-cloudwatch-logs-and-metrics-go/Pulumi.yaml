name: ${PROJECT}
runtime: go
description: ${DESCRIPTION}
template:
  description: Enabling CloudWatch Logs and Metrics
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
