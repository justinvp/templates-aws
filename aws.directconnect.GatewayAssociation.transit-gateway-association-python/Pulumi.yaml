name: ${PROJECT}
runtime: python
description: ${DESCRIPTION}
template:
  description: Transit Gateway Association
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
