name: ${PROJECT}
runtime: go
description: ${DESCRIPTION}
template:
  description: Aurora with PostgreSQL engine
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
