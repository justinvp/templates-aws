name: ${PROJECT}
runtime: python
description: ${DESCRIPTION}
template:
  description: Add notification configuration to SQS Queue
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
