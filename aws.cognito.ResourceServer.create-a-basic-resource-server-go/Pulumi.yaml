name: ${PROJECT}
runtime: go
description: ${DESCRIPTION}
template:
  description: Create a basic resource server
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
