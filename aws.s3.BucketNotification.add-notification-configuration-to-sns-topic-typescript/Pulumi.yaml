name: ${PROJECT}
runtime: nodejs
description: ${DESCRIPTION}
template:
  description: Add notification configuration to SNS Topic
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
