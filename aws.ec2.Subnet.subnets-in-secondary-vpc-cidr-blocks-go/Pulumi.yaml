name: ${PROJECT}
runtime: go
description: ${DESCRIPTION}
template:
  description: Subnets In Secondary VPC CIDR Blocks
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
