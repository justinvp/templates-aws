name: ${PROJECT}
runtime: dotnet
description: ${DESCRIPTION}
template:
  description: Amazon Cognito domain
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
