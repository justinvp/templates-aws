name: ${PROJECT}
runtime: go
description: ${DESCRIPTION}
template:
  description: Google Cloud Messaging (GCM)
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
