name: ${PROJECT}
runtime: go
description: ${DESCRIPTION}
template:
  description: Volume Gateway (Stored)
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
