name: ${PROJECT}
runtime: dotnet
description: ${DESCRIPTION}
template:
  description: Aurora Multi-Master Cluster
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
