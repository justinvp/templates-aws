name: ${PROJECT}
runtime: python
description: ${DESCRIPTION}
template:
  description: Multiple Node Master Instance Group
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
