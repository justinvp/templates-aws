name: ${PROJECT}
runtime: go
description: ${DESCRIPTION}
template:
  description: With Latest Version Of Launch Template
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
