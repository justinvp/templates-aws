name: ${PROJECT}
runtime: nodejs
description: ${DESCRIPTION}
template:
  description: Creating A New Key Pair
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
