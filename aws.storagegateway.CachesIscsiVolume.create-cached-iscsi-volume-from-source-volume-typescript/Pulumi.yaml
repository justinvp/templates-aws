name: ${PROJECT}
runtime: nodejs
description: ${DESCRIPTION}
template:
  description: Create Cached iSCSI Volume From Source Volume
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
