name: ${PROJECT}
runtime: python
description: ${DESCRIPTION}
template:
  description: An AWS EnvironmentEC2 Python Pulumi program
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
