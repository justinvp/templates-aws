name: ${PROJECT}
runtime: go
description: ${DESCRIPTION}
template:
  description: An AWS ClientCertificate Go Pulumi program
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
