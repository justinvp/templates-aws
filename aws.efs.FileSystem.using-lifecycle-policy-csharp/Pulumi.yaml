name: ${PROJECT}
runtime: dotnet
description: ${DESCRIPTION}
template:
  description: Using lifecycle policy
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
