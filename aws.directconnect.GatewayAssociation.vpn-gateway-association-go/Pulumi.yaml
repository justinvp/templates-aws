name: ${PROJECT}
runtime: go
description: ${DESCRIPTION}
template:
  description: VPN Gateway Association
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
