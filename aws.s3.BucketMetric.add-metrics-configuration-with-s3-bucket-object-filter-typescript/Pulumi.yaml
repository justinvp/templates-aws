name: ${PROJECT}
runtime: nodejs
description: ${DESCRIPTION}
template:
  description: Add metrics configuration with S3 bucket object filter
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
