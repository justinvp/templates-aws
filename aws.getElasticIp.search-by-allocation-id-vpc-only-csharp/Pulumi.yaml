name: ${PROJECT}
runtime: dotnet
description: ${DESCRIPTION}
template:
  description: Search By Allocation ID (VPC only)
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
