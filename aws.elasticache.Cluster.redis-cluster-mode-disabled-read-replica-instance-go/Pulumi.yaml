name: ${PROJECT}
runtime: go
description: ${DESCRIPTION}
template:
  description: Redis Cluster Mode Disabled Read Replica Instance
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
