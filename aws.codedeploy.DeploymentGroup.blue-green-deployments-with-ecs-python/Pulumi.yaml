name: ${PROJECT}
runtime: python
description: ${DESCRIPTION}
template:
  description: Blue Green Deployments with ECS
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
