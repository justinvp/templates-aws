name: ${PROJECT}
runtime: dotnet
description: ${DESCRIPTION}
template:
  description: List all account IDs for the organization
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
