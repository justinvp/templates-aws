name: ${PROJECT}
runtime: go
description: ${DESCRIPTION}
template:
  description: By Transit Gateway and VPN Connection Identifiers
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
