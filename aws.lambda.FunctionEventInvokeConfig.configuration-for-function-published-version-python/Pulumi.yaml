name: ${PROJECT}
runtime: python
description: ${DESCRIPTION}
template:
  description: Configuration for Function Published Version
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
