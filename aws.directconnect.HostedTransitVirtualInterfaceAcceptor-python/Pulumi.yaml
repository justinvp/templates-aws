name: ${PROJECT}
runtime: python
description: ${DESCRIPTION}
template:
  description: An AWS HostedTransitVirtualInterfaceAcceptor Python Pulumi program
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
