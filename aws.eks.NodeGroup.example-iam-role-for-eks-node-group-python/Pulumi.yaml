name: ${PROJECT}
runtime: python
description: ${DESCRIPTION}
template:
  description: Example IAM Role for EKS Node Group
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
