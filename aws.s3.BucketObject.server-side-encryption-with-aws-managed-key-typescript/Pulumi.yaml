name: ${PROJECT}
runtime: nodejs
description: ${DESCRIPTION}
template:
  description: Server Side Encryption with AWS-Managed Key
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
