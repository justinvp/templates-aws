name: ${PROJECT}
runtime: nodejs
description: ${DESCRIPTION}
template:
  description: Route53 Record
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
