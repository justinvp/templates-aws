name: ${PROJECT}
runtime: go
description: ${DESCRIPTION}
template:
  description: Lambda Target Group
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
