name: ${PROJECT}
runtime: nodejs
description: ${DESCRIPTION}
template:
  description: Example IAM Setup in Target Account
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
