name: ${PROJECT}
runtime: nodejs
description: ${DESCRIPTION}
template:
  description: EFS File System w/ tags
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
