name: ${PROJECT}
runtime: dotnet
description: ${DESCRIPTION}
template:
  description: On-Demand Trigger
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
