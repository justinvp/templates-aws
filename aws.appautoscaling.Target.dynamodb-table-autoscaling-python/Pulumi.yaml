name: ${PROJECT}
runtime: python
description: ${DESCRIPTION}
template:
  description: DynamoDB Table Autoscaling
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
