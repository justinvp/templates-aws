name: ${PROJECT}
runtime: nodejs
description: ${DESCRIPTION}
template:
  description: An AWS getOutpost TypeScript Pulumi program
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
