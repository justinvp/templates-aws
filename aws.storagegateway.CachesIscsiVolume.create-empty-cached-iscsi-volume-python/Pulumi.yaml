name: ${PROJECT}
runtime: python
description: ${DESCRIPTION}
template:
  description: Create Empty Cached iSCSI Volume
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
