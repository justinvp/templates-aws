name: ${PROJECT}
runtime: python
description: ${DESCRIPTION}
template:
  description: Logging Individual S3 Bucket Events
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
