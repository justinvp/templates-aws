name: ${PROJECT}
runtime: nodejs
description: ${DESCRIPTION}
template:
  description: Catalog Target
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
